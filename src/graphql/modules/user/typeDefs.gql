type User {
  _id: ID!
  username: String!
}

type UserRegistred {
  user: User!
  token: String!
}

type Query {
  loginUser(username: String!, password: String!): String
}

type Mutation {
  registerUser(username: String!, password: String!): UserRegistred!
}
